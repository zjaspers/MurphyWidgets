function setNow(){document.querySelectorAll('[data-now]').forEach(e=>e.textContent=new Date().toLocaleString([], {month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}));}
setNow(); setInterval(setNow,60000);
function completePumpTask(){const status=document.querySelector('#pump9Status'); const action=document.querySelector('#pump9Action'); const flow=document.querySelector('#pump9Flow'); const badge=document.querySelector('#pump9Badge'); if(status){status.textContent='Pending Verification'; status.className='status warn'; status.innerHTML='<span class="dot"></span>Pending Verification';} if(action)action.textContent='Filter replaced in WorkJam · awaiting system flow update'; if(flow)flow.textContent='5.2 GPM'; if(badge){badge.textContent='Pending'; badge.className='status warn';} document.querySelectorAll('.pump9card').forEach(p=>{p.querySelector('.status')?.remove(); p.insertAdjacentHTML('beforeend','<div style="margin-top:8px"><span class="status warn"><span class="dot"></span>Pending</span></div>')});}
function markSkuChecked(){const el=document.querySelector('#skuState'); if(el){el.className='status good'; el.innerHTML='<span class="dot"></span>Recovery task completed';} const gap=document.querySelector('#recoveryGap'); if(gap) gap.textContent='$3,105';}
function markOperational(){const e=document.querySelector('#printerStatus'); if(e){e.className='status warn'; e.innerHTML='<span class="dot"></span>Pending Vendor Dispatch';} }
